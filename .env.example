# Copy this file to apps/web/.env and update the values

# Database URL for Docker Compose PostgreSQL
DATABASE_URL="postgresql://collboard:collboard@localhost:5432/collboard?schema=public"

# NextAuth.js secrets (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Redis URL for real-time features
REDIS_URL="redis://localhost:6379"
