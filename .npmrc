# Optimize npm install performance
prefer-offline=true
audit=false
fund=false
progress=false
fetch-timeout=120000

# Skip Playwright browser download during install (run `npx playwright install` manually for e2e)
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
